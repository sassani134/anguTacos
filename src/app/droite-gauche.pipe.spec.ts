import { DroiteGauchePipe } from './droite-gauche.pipe';

describe('DroiteGauchePipe', () => {
  it('create an instance', () => {
    const pipe = new DroiteGauchePipe();
    expect(pipe).toBeTruthy();
  });
});
